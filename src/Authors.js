import React, { Component } from 'react';

export class Authors extends Component {

    render() {
        return(
            <h1>Authors.js is working</h1>
        )
        
    }
}

//Didn't need this component, just messed around with it and it works.

export default Authors